## R CMD check results

0 errors | 0 warnings | 0 notes

## Notes for v1.1

* Excluded `.github` directory from package tarball via `.Rbuildignore`
* Declared `id` as a global variable to resolve R CMD check NOTE about no
  visible binding for global variable in NSE contexts
